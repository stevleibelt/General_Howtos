# list
git tag -l

# list with pattern
git tag -l 'v1.2.3.*'

# checkout
git checkout tags/<tag_name>

# create
git tag -a v1.2.3 -m 'version v1.2.3 created because of feature freeze'

# delete
git tag -d v1.2.3
git push orgin :refs/tags/v1.2.3

# show information
git show v1.2.3

# sign with gpg
git tag -s v1.2.3 -m 'signed version1.2.3'

# push to repository
git push orgin v1.2.3

# links
* http://git-scm.com/book/de
* http://git-scm.com/book/de/Git-Grundlagen-Tags
* http://nathanhoad.net/how-to-delete-a-remote-git-tag
