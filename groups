# list groups per user
groups $user
# add user to group
usermod -a -G $group,$group $user
# add groupid
chmod -R g+s $path
# add group sticky bit
chmod -R 2775 $path
# add sticky bit
chmod -R +s $path
