#<minute> <hour> <day_of_month> <month> <day_of_week> <command>

* * * * * echo "hello world" && echo "ok" || echo "failed" >> /var/log/cronjob.log 2>&1
# remember to end this file with an empty new line
