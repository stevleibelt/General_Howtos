-- as long as the connection is open, this temp table exists.
-- any way it is common practice to remove the temp table when you are finished with it.

CREATE TEMPORARY TABLE my_table;

-- work with the temp table

DROP TEMPORARY TABLE my_table;
