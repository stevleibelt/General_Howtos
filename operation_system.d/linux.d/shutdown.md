# shutdown system in 60 minutes and going to halt

    ```
    shutdown +60 -h
    ```
# shutdown with message

    ```
    shutdown 'message'
    ```
# shutdown system now and going to reboot

    ```
    shutdown now -r
    ```

# cancle a shotdown

    ```
    shutdown -c
    ```
