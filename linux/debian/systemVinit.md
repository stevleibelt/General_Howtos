# general

* start/stop scripts are located below "/etc/init.d/".
* they can be executed by a given runlevel when you link them in "/etc/rc(1-6).d/"
* you can use "update-rc.d" to do this linking automatically
