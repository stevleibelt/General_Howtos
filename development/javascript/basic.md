If Is Undefined
---------------

if (myVariable === undefined) {
}

If Is A Function
----------------

if (typeof callback === "function") {
}

Default Value
-------------

function foobar(foo)
{
    var foo = foo || 'bar';
    //...
}
